#!/bin/bash

for config in $( ls /home/cs2/conrada/Catterwampus/configs/real_RM_DM_* )
do

echo "java gelations.PerformExperiment $config"
java gelations.PerformExperiment $config

done

for config in $( ls /home/cs2/conrada/Catterwampus/configs/real_RM_EM_* )
do

echo "java gelations.PerformExperiment $config"
java gelations.PerformExperiment $config

done
for config in $( ls /home/cs2/conrada/Catterwampus/configs/real_RM_ISM_* )
do

echo "java gelations.PerformExperiment $config"
java gelations.PerformExperiment $config

done
for config in $( ls /home/cs2/conrada/Catterwampus/configs/real_RM_IVM_* )
do

echo "java gelations.PerformExperiment $config"
java gelations.PerformExperiment $config

done
for config in $( ls /home/cs2/conrada/Catterwampus/configs/real_RM_SIM_* )
do

echo "java gelations.PerformExperiment $config"
java gelations.PerformExperiment $config

done
for config in $( ls /home/cs2/conrada/Catterwampus/configs/real_RM_SM_* )
do

echo "java gelations.PerformExperiment $config"
java gelations.PerformExperiment $config

done

exit 0